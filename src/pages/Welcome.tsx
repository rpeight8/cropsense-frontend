import { useParams, useNavigate } from "react-router-dom";

export const Welcome = () => {
  return (
    <div className="text-6xl text-white flex justify-center items-center w-full">
      Hello 👋
    </div>
  );
};
